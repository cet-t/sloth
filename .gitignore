# Rust
/target
**/*.rs.bk
# Cargo.lock is committed: this workspace ships binaries (rmap-daemon/-config),
# so a locked, reproducible dependency set is desirable.

# OS
.DS_Store
Thumbs.db

# User data
user/
!user/.gitkeep

# IDE
.idea/
.vscode/
*.swp

# Local agent / tooling state
.claude/
tasks/
build.bat

# Build artifacts
*.exe
*.dll
*.dylib
*.so
*.app

token-optimizer/
